# Supabase Configuration
# CRITICAL: These MUST be set in Vercel Dashboard → Settings → Environment Variables
# Set for ALL environments: Production, Preview, AND Development

# Supabase Project URL (find in Supabase Dashboard → Settings → API)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Service Role Key (SECRET - find in Supabase Dashboard → Settings → API)
# Used for admin operations (bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Anon Key (PUBLIC - find in Supabase Dashboard → Settings → API)
# Used for client-side operations (respects RLS)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_... or sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_test_... or pk_live_...

# Meta/Facebook Configuration (for webhooks)
META_VERIFY_TOKEN=orderflow-webhook-verify
META_APP_SECRET=your-app-secret
META_PAGE_ACCESS_TOKEN=your-page-access-token

# IMPORTANT NOTES:
# 1. Never commit actual values to git
# 2. Set these in Vercel Dashboard for EACH environment
# 3. After setting, redeploy to apply changes
# 4. Variables are NOT automatically copied between environments
